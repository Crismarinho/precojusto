Instalação cypress 
Instalar 

1. Git bash
2. Node.js 
3. Google Chrome 
4. Vs Code 



No Git bash acessar a pasta do projeto caso já tenha um projeto 
- npm init -> Esse comando inicia as dependências iniciar os projeto do cypress
- npm install cypress -> Comando para instalar o cypress 
- npx cypress open -> Comando para Abrir o Cypress  

mkdir <****> Comando para criar um projeto para iniciar o cypress quando não há um projeto existente. 


  import { getCLS } from 'web-vitals';

describe.only('Gerar CPF', () => {

  it.only('Usuario acessa a página e gera CPF com "7"', () => {
    //Aqui está acessando a página
    cy.visit('https://www.4devs.com.br/gerador_de_cpf')

    //Aqui usuario seleciona a opção sem pontuação
    //cy.get('input[type="radio"][name="pontuacao"][id="pontuacao_nao"][value="N"]').click()
    //cy.get('#pontuacao_nao').click()
    //cy.get('label[atributo="valor"] .checkmark').click()
    //cy.get(':nth-child(3) > label > .checkmark').click

    //Aqui usuario seleciona o estado de origem
    cy.get('#cpf_estado').select ('AM')
    
    //Aqui usuario clica no botão para gerar o CPF que inicie com '7'
    //cy.get('#bt_gerar_cpf').click

  })
});